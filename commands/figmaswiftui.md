---
description: Implement a Figma frame as production SwiftUI, grounded in real design values and the project's actual design system, architecture, and concurrency model — not generic SwiftUI
---

Apply the `figma-to-swiftui` skill to the Figma selection/URL below. That skill
requires `figma-grounding` to have gated value extraction first — its own procedure
invokes that inline as step 1, so don't invoke `figma-grounding` separately, just let
`figma-to-swiftui` pull it in.

Do not skip pre-flight (project context/architecture/concurrency check) or Phase C
(validation against the Figma screenshot) even if this looks like a simple screen.

Task: $ARGUMENTS
