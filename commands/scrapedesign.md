---
description: Ground Claude in the real Figma design (exact tokens, not screenshot guesses) before executing whatever you ask for in this message
---

Before doing anything else, apply the `figma-grounding` skill to whatever Figma selection/URL is relevant here. Pull real values via MCP first. Do not skip this because the request seems simple.

Then execute the actual task below, using only grounded values.

Task: $ARGUMENTS
