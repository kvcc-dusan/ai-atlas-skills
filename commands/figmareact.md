---
description: Implement a Figma frame as production React + TypeScript, grounded in real design values and the project's actual design system, styling approach, and conventions — not generic JSX
---

Apply the `figma-to-react` skill to the Figma selection/URL below. That skill requires
`figma-grounding` to have gated value extraction first — its own procedure invokes that
inline as step 1, so don't invoke `figma-grounding` separately, just let
`figma-to-react` pull it in.

Do not skip pre-flight (MCP-server check, styling approach, project context) or Phase C
(validation against the Figma screenshot) even if this looks like a simple screen.

Task: $ARGUMENTS
